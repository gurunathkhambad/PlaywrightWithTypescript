typescript file always be convert the from the typescript to the javascript. Without converstion 
typescript will not run directly. 

#For running the ts file below command can use
tsc filename.ts

#For running the js file below command can use
node filename.js

# Running the typescript file directly without running the tsc filename.ts and node filename.js command we can use below command 

tsx filename.ts


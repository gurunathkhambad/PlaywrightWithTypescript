
enum Browser{
    Chrome="chrome",
    Edge="edge",
    Firefox="firefox"
}

let browserName:Browser=Browser.Edge;
console.log(browserName);

enum Status{
    completed,
    inProgress,
    pending
}

let latestStatus:Status=Status.inProgress;
console.log(latestStatus);